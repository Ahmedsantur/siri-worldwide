name: Build SIRI APK
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
      - run: pnpm install --no-frozen-lockfile
      - run: pnpm --filter siri-worldwide run build
        env:
          PORT: 3000
      - run: pnpm --filter siri-worldwide exec cap add android
        continue-on-error: true
      - run: pnpm --filter siri-worldwide exec cap sync android
      - run: cd siri-worldwide/android && chmod +x gradlew &&./gradlew assembleDebug
      - uses: actions/upload-artifact@v4
        with:
          name: SIRI-Worldwide-APK
          path: siri-worldwide/android/app/build/outputs/apk/debug/app-debug.apk
