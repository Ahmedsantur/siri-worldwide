# Firebase Firestore REST configuration
# Create a Firebase web app, enable Firestore, and add these values to the
# SIRI Worldwide environment to sync the same conversation across devices.
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_API_KEY=